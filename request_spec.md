# YouTube 영상 질의응답 도구 기획서

## 1. 개요

### 1.1 목적
YouTube 영상 URL과 미리 저장해 둔 질문을 조합해, NotebookLM(Gemini Notebook)의 근거 기반 답변을 받아보는 개인용 도구를 만든다.

### 1.2 배경
자주 쓰는 질문("핵심 주장 3가지 정리", "발표자의 결론은?" 등)을 매번 타이핑하는 대신 등록해 두고 선택만 하면 되도록 한다. 영상마다 NotebookLM 웹 UI에서 노트북을 만들고 소스를 붙이는 반복 작업을 자동화하는 것이 핵심이다.

### 1.3 범위
- 포함: 단일 YouTube 영상에 대한 1회성 질의응답, 질문 템플릿 관리
- 제외: 다중 사용자 지원, 대화형 후속 질문, 오디오/비디오 등 스튜디오 아티팩트 생성

---

## 2. 사용 시나리오

1. 사용자가 브라우저에서 도구를 연다.
2. YouTube URL을 붙여넣는다.
3. 콤보박스에서 저장된 질문 하나를 고른다.
4. `실행` 버튼을 누른다.
5. 진행 상태가 표시되고, 잠시 후 답변이 화면에 출력된다.
6. 필요하면 `질문 관리` 탭에서 질문을 추가·수정·삭제한다.

---

## 3. 기능 요구사항

| ID | 기능 | 설명 | 우선순위 |
|---|---|---|---|
| F-01 | YouTube URL 입력 | 텍스트 입력란. 형식 유효성 검사 | 필수 |
| F-02 | 질문 선택 | 저장된 질문 목록을 콤보박스로 표시 | 필수 |
| F-03 | 질의 실행 | URL + 질문으로 NotebookLM 응답 요청 | 필수 |
| F-04 | 답변 출력 | 답변 본문 표시 | 필수 |
| F-05 | 질문 조회 | 저장된 질문 목록 표시 | 필수 |
| F-06 | 질문 추가 | 새 질문 등록 | 필수 |
| F-07 | 질문 수정 | 기존 질문 내용 변경 | 필수 |
| F-08 | 질문 삭제 | 질문 제거 | 필수 |
| F-09 | 진행 상태 표시 | 처리 중임을 스피너로 안내 | 필수 |
| F-10 | 임시 노트북 정리 | 남은 임시 노트북 일괄 삭제 | 권장 |
| F-11 | 인용 출처 표시 | 답변의 근거 구간 함께 표시 | 선택 |
| F-12 | 결과 이력 저장 | 과거 질의 결과 재조회 | 선택 |

---

## 4. 비기능 요구사항

- **응답 시간**: 소스 인덱싱 때문에 수십 초가 걸릴 수 있다. 즉시 응답을 전제하지 않고, 대기 UI를 반드시 제공한다.
- **인증**: 최초 1회 `notebooklm login`으로 로컬에 쿠키를 저장한다. 이후 별도 로그인 절차 없음.
- **가용성**: 개인용이므로 무중단 요건 없음. 필요할 때 실행한다.
- **보안**: 로컬에서만 동작하며 외부 노출 없음(127.0.0.1 바인딩).

---

## 5. 기술 구성

### 5.1 스택

| 구분 | 선택 | 선정 이유 |
|---|---|---|
| UI | Streamlit | 콤보박스·스피너·탭이 기본 제공, 코드량 최소 |
| 백엔드 | Python (asyncio) | notebooklm-py가 async 기반 |
| NotebookLM 연동 | notebooklm-py (버전 고정) | 비공식 API라 마이너 업데이트로도 깨질 수 있음 |
| 저장소 | SQLite | 파일 1개, 서버 불필요. 데이터 규모가 작음 |

### 5.2 파일 구성

```
project/
├─ app.py          # Streamlit 화면 (질의 탭 / 질문 관리 탭)
├─ nlm.py          # NotebookLM 연동 및 임시 노트북 정리
├─ questions.py    # SQLite CRUD
└─ questions.db    # 데이터 파일
```

---

## 6. 처리 흐름

```
[URL + 질문 입력]
      ↓
노트북 생성  notebooks.create("tmp-xxxxxxxx")
      ↓
소스 추가    sources.add_url(url, wait=True)   ← 자막 없으면 여기서 실패
      ↓
질의         chat.ask(question)
      ↓
답변 출력
      ↓
노트북 삭제  notebooks.delete()   ← 실패 여부와 무관하게 항상 실행
```

**설계 근거**: "한 영상당 한 질문" 사용 패턴이므로 노트북을 재사용할 이유가 없다. 매번 새로 만들고 지우는 방식이 다음 문제를 한꺼번에 해소한다.

- 노트북당 소스 개수 상한(Standard 50개)에 도달하지 않음
- 이전 영상 내용이 섞인 오염된 답변이 나오지 않음
- 소스 단위 삭제 API의 존재 여부를 확인할 필요가 없음

임시 노트북에는 `tmp-` 접두어를 붙여, 정리 기능이 대상을 식별할 수 있게 한다.

---

## 7. 데이터 모델

### questions

| 컬럼 | 타입 | 설명 |
|---|---|---|
| id | INTEGER PK | 자동 증가 |
| text | TEXT NOT NULL | 질문 본문 |
| created_at | TEXT | 등록 시각 |
| updated_at | TEXT | 수정 시각 |

F-12(결과 이력)를 구현할 경우 `history(id, url, question, answer, created_at)` 테이블을 추가한다.

---

## 8. 화면 구성

### 탭 1. 질의
- YouTube URL 입력란
- 질문 선택 콤보박스
- 실행 버튼
- 진행 표시 영역
- 답변 출력 영역

### 탭 2. 질문 관리
- 질문 목록 (조회)
- 추가 입력란 + 등록 버튼
- 항목별 수정 / 삭제 버튼

---

## 9. 예외 처리

| 상황 | 처리 |
|---|---|
| 자막 없는 영상 | "자막이 없어 처리할 수 없는 영상입니다" 안내. 오류가 아닌 정상 결과로 취급 |
| 비공개·연령제한·라이브 영상 | 위와 동일하게 사유 안내 |
| 잘못된 URL 형식 | 요청 전 입력 단계에서 차단 |
| 인증 만료 | "터미널에서 `notebooklm login`을 다시 실행하세요" 안내 |
| 요청 한도 초과 | 재시도 안내 메시지 표시 |
| 처리 중 예외 발생 | 임시 노트북은 반드시 삭제 후 오류 표시 |

---

## 10. 제약사항 및 리스크

- **비공식 API 의존**: notebooklm-py는 Google이 문서화하지 않은 내부 엔드포인트를 사용한다. 예고 없이 동작이 멈출 수 있으므로 버전을 고정하고, 업데이트 시 동작을 재확인한다.
- **자막 의존**: NotebookLM은 영상 자체가 아니라 자막을 읽는다. 자막 품질이 답변 품질을 좌우한다.
- **약관**: 자동화 접근은 Google 약관상 회색지대다. 개인 용도의 소량 사용으로 제한하고, 필요하면 별도 계정을 쓴다.
- **답변 정확도**: 인용이 붙어도 오류가 있을 수 있다. 중요한 내용은 원본 영상으로 확인한다.
- **임시 노트북 누적**: 프로세스가 비정상 종료되면 정리되지 않은 노트북이 남는다. F-10으로 대응한다.

---

## 11. 개발 단계

### 1단계 — 동작 확인
`nlm.py`만 만들어 스크립트로 실행. URL 하나에 대해 생성→질의→삭제가 끝까지 도는지 확인한다.

### 2단계 — MVP
F-01 ~ F-09 구현. 이 시점부터 실사용 가능.

### 3단계 — 안정화
F-10 정리 기능, 예외 메시지 정돈.

### 4단계 — 선택
F-11 인용 표시, F-12 이력 저장.

---

## 12. 미결 사항

- 답변 출력 형식: 화면 표시만으로 충분한지, 파일 저장(Markdown 등)이 필요한지
- 인용 출처(F-11)를 MVP에 포함할지 여부